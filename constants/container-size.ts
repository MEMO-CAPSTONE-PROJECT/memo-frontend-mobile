import { ContainerType } from "@/shared/types/container-type";

export const ContainerSize: ContainerType = {
    none: "",
    medium: "justify-center items-center w-20 h-20",
    full: "w-full h-full",
}